---
date: 2026-07-02
weekday: 木曜日
type: daily
tags:
  - 日記
  - COSA
  - Claude Code
---

## 体とパワー（本線）

- **本線**: —
- **ひとこと**:

## 筋トレ（任意）

- **記録**: —

## 運動（7〜8月はホットヨガ）

- **外に出た**: —
- **ホットヨガ**: —
- **家で動いた**: ○（ピラティス · 柔軟）
- **就寝**（任意）: —

## 食事準備（任意 · 7月〜）

- **食準備**: —

---

## 予定

無し

## ログ

- COSA vault を Cursor で開き、README からプロジェクトの目的を確認した
- ターミナルを開き、Claude Code（`@anthropic-ai/claude-code`）をインストールした
- Claude Code の OAuth ログインを完了した（ブラウザ認証 → コード貼り付け）
- Claude Code の使い方を確認した（入力欄 `>`、`/theme` で Light mode、Cursor チャットとの使い分け）
- 家でピラティス（柔軟）をした（翌日の日記より）
- UDEV Gothic フォントをダウンロードし、Cursor のエディタ／ターミナルのフォント設定（`terminal.integrated.fontFamily` / `fontWeight` / `letterSpacing`）を確認・変更した
- 4ペインアプリのフォルダを探し、`package.json` のあるフォルダへ移動して `npm run dev` を実行した
- Mac 内から「台本ワークスペース」関連プロジェクトを探索。候補2件（うち `workspace-ui-kit`）のフォルダ名・最終更新日時・`memory-architecture.html` / `package.json` の場所・Vercel 対応有無を一覧化した
- `workspace-ui-kit` の `app` / `components` / `lib` を確認し、「台本」「script」「workspace」「junin」関連の画面を一覧化した
- `workspace-ui-kit` を `npm install` → `npm run dev` で起動し、`http://localhost:3000` で確認した
- 7月のAIスクール課題（`git-workspace1.vercel.app` / `memory-architecture.html`）を確認した

## 考えたこと

- ターミナル版 Claude Code はクリックでは進まず、キーボード操作が前提だと分かった
- パスワードはチャットに送っていない（Mac の sudo 入力もログに残らない）ことを確認して安心した
- 会話・説明は Cursor チャット、ファイル操作は Claude Code、という使い分けが読みやすさの面でもよさそう

## リンク

- 前: [[日記/2026-07-01]]
- 次: [[日記/2026-07-03]]
